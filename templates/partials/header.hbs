{{!-- templates/partials/header.hbs --}}
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{subject}} - {{siteName}}</title>
  <style>
    {{> styles}}
    .btn {
      display: inline-block;
      background: #1e81b0;
      color: white !important;
      text-decoration: none;
      padding: 12px 20px;
      border-radius: 6px;
      font-weight: bold;
      margin: 15px 0;
    }
    .box {
      margin: 16px 0;
      padding: 14px;
      border-left: 4px solid #1e81b0;
      background: #f1f7fb;
      border-radius: 4px;
    }
  </style>
</head>
<body style="margin: 0; padding: 0; background: #f7f8fb; color: #333; font-family: Arial, sans-serif; line-height: 1.6;">
  <div style="max-width: 600px; margin: 0 auto; padding: 20px;">
    <div style="background: white; border: 1px solid #eee; border-radius: 8px; overflow: hidden; box-shadow: 0 1px 2px rgba(16,24,40,.04);">
      <div style="text-align: center; padding: 24px 20px; border-bottom: 1px solid #eee; background: white;">
        <img src="{{logoUrl}}" alt="{{siteName}}" style="max-width: 110px; height: auto; display: block; margin: 0 auto 8px;">
      </div>
      <div style="padding: 20px;">
